plugins {
    id 'eclipse'
    id 'idea'
    id 'net.minecraftforge.gradle' version '[6.0,6.2)'
    id 'com.diffplug.eclipse.apt'
    id 'org.spongepowered.mixin'
    id 'com.modrinth.minotaur' version '2.+'
}

group= "team.creative" + project.mod_id

java.toolchain.languageVersion = JavaLanguageVersion.of(17)

java.sourceSets {
    main {
        java {
            srcDirs = ["src/main/java", "src/forge/java"]
        }
        resources {
            srcDirs = ["src/main/resources", "src/forge/resources"]
        }
    }
}


minecraft {
    mappings channel: 'official', version: "${project.property 'minecraft_version'}"
    
    runs {
        client {
            taskName "nodurabilitylost_client"
            workingDirectory project.file('run')
            
            property 'forge.logging.console.level', 'debug'

            mods {
                "${mod_id}" {
                    source sourceSets.main
                }
            }
        }

        server {
            taskName "nodurabilitylost_server"
            workingDirectory project.file('run')
            
            property 'forge.logging.console.level', 'debug'

            mods {
                "${mod_id}" {
                    source sourceSets.main
                }
            }
        }
    }
}

dependencies {
    minecraft "net.minecraftforge:forge:${project.property 'minecraft_version'}-${project.property 'forge_version'}"
    
    annotationProcessor 'org.spongepowered:mixin:0.8.5:processor'
}

modDependencies = []

mixin {
    add sourceSets.main, 'nodurabilitylost.mixin.refmap.json'
     
    config 'nodurabilitylost.mixins.json'
}
